class Notification() :

    # Constructor
    def __init__( self ) -> None:
        pass

    def send_email( name: str, email: str, type = "" ) :

        print( f"Sending message to { name } via, { email } with the title { type }" )
        pass